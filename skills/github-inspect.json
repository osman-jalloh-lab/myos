{
  "name": "github.inspectRepo",
  "description": "Inspect a GitHub repository — fetch metadata, language, stars, forks, topics, license, and README preview. Pass a full GitHub URL or a message containing one.",
  "category": "development",
  "agent": "argus",
  "inputs": {
    "message": {
      "type": "string",
      "description": "Natural language message containing a GitHub URL, e.g. 'inspect https://github.com/owner/repo'"
    },
    "repoUrl": {
      "type": "string",
      "description": "Direct GitHub repo URL (optional — extracted from message if omitted)"
    }
  },
  "required": ["message"],
  "execution": {
    "tool": "internal.github.inspectRepo",
    "risk": "read",
    "requiresApproval": false
  },
  "whenToUse": [
    "Osman pastes a GitHub URL and wants a quick read on the project.",
    "Osman asks about a repo's language, activity, or license before using it.",
    "Research on a tool or dependency that lives on GitHub."
  ],
  "whenNotToUse": [
    "Do not use to clone, edit, build, or run repo code. That is the builder and worker path.",
    "Do not use for private repos or anything needing auth beyond public metadata."
  ],
  "examples": [
    "inspect https://github.com/vercel/next.js",
    "what is this repo https://github.com/anthropics/anthropic-sdk-python",
    "tell me about https://github.com/owner/repo"
  ]
}
